# Disable H2 or any embedded database (since we're using Supabase)
#spring.datasource.url=jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:6543/postgres
#spring.datasource.username=postgres.kutxlfnyhecasfmxnjba
#spring.datasource.password=cbds8iy4wbrefjds
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Ensure Hibernate manages schema correctly for Supabase
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Ensure that the SQL scripts (if any) run on test startup
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true